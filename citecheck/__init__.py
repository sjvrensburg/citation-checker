"""citecheck — verify academic citations against canonical scholarly sources.

Zero third-party dependencies: everything runs on the Python 3.8+ standard library
so the tool behaves identically under Claude Code, Codex, CI, or a bare shell.
"""

__version__ = "0.1.0"
